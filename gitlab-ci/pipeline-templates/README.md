# GitLab CI Pipeline Templates

This repository contains reusable GitLab CI pipeline templates used across projects in the `devops` group. These templates provide shared logic for tasks such as AWS authentication, Terraform execution, TechDocs publishing, and more.
