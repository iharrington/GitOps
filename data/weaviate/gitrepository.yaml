apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: weaviate
  namespace: flux-system
spec:
  interval: 1m
  url: https://gitlab.com/platform/data/weaviate.git
  ref:
    branch: main
  secretRef:
    name: platform-gitlab-credentials
