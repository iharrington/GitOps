apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: jena-fuseki
  namespace: flux-system
spec:
  interval: 1m
  url: https://gitlab.com/platform/data/jena-fuseki.git
  ref:
    branch: main
  secretRef:
    name: platform-gitlab-credentials
